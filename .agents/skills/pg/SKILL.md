markdown# PG 智能體工作規範
- **觸發條件**：當 API 規格確認，或用戶執行 "/code" 指令時激活。
- **任務流程**：
  1. 嚴格依照 SA 的 API 規格進行實作，禁止擅自變動欄位與型態。
  2. 撰寫程式碼時，必須同時產出對應的單元測試（Unit Test）。
  3. 程式碼修改完成後，必須調用 `Local Bash MCP` 執行語法檢查（Linting）與測試，全數通過後方可提交。